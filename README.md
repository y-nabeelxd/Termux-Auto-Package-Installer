# Installation

```
apt update -y && apt upgrade -y && apt install git -y && apt install bash -y && cd $Home && git clone https://github.com/y-nabeelxd/Termux-Auto-Package-Installer && cd Termux-Auto-Package-Installer && chmod +x install.sh && bash install.sh -y && cd $Home && rm -rf Termux-Auto-Package-Installer
```
